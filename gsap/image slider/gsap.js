const tl = gsap.timeline()
const tl1 = gsap.timeline()

tl.to("#one",{
    width: '100%',
    duration: 3,
})


tl.to("#two",{
    height: '100%',
    duration: 3,
})

tl.to("#three",{
    width: '100%',
    duration: 3,
})

